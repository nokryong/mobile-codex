{
  "name": "mobile-codex-ui-tests",
  "version": "0.1.6-alpha",
  "private": true,
  "scripts": { "test": "node --test tests/*.test.cjs" },
  "devDependencies": { "jsdom": "26.1.0" },
  "engines": { "node": ">=20" }
}
